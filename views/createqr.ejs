<%- include('header.ejs') %>
    <% if (saisie) { %>
        <div class="container-fluid align-items-center">
            <div class="form-group">

                <form action="/createqr/scan" method="POST" class="form" style="margin-left:30%; margin-right:30%">

                    <div class="form-group">
                        <label for="identite">Identité</label>
                        <br>
                        <select name="identite" id="identite">
                            <option value="" disabled selected>Selectionner un intervenant</option>
                            <option value="Yannick Midey">Yannick Midey</option>
                            <option value="Lucas Pisano">Lucas Pisano</option>
                            <option value="Alexandre Alleaume">Alexandre Alleaume</option>
                            <option value="Bryan Guillot">Bryan Guillot</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="code">Code de sécurité :</label>
                        <input type="password" class="form-control" name="code" id="code" placeholder="Entrez votre code à l'abri des regards..." required>
                    </div>

                    <div class="form-group">
                        <label for="salle">Salle</label>
                        <input type="number" class="form-control" name="salle" id="salle" placeholder="Entrez la salle d'intervention" required>
                    </div>
                    <div class="form-group">
                        <label for="horaireEntree">Horaire d'entrée</label>
                        <input type="datetime-local" class="form-control" name="horaireEntree" id="horaireEntree" placeholder="Entrez l'heure de sortie" required>
                    </div>

                    <div class="form-group">
                        <br/>
                        <button type="submit" class="btn btn-outline-primary">Générer le QRCode</button>
                    </div>
                </form>

                <% } else { %>
                    <div class="qr-code">
                        <h3>Voici le QR Code généré :</h3>
                        <img id="qrcode" src=<%=qr_code %> alt="QR Code"></div>
                    <% } %>